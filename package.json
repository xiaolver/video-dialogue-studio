{
  "name": "video-dialogue-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "helper": "node scripts/youtube-helper.mjs",
    "helper:chrome": "node scripts/youtube-helper.mjs --browser chrome",
    "helper:edge": "node scripts/youtube-helper.mjs --browser edge",
    "deploy": "wrangler deploy",
    "deploy:one-click": "node scripts/deploy.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.113.0"
  }
}
